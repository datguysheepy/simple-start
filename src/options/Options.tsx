import { FC } from 'react';
import './styles.css';

const Options: FC = () => {
	return (
		<div>
			<header>
				<p>options page wooho! tak?</p>
			</header>
		</div>
	);
};

export default Options;
